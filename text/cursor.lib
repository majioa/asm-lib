AT	EQU	PUT_CURSOR
PUT_CURSOR	PROC;IN AX-CURS
	PUSH	BX
	PUSH	DX
	XOR	BH,BH
	MOV	DX,AX
	MOV	AH,2
	INT	10H
	POP	DX
	POP	BX
	RET
ENDP
GET_CURSOR  PROC;OUT AX-CURS COODRS ,CX - CURS LEN
	PUSH	BX
	PUSH	DX
	MOV	AH,3
	XOR	BH,BH
	INT	10H
	MOV	AX,DX
	POP	DX
	POP	BX
	RET
ENDP